require 'spec_helper'

describe Cap::Rm::Deploy do
  it 'has a version number' do
    expect(Cap::Rm::Deploy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
